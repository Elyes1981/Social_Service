import { CollaborationStatus } from './collaboration-status.model';

describe('CollaborationStatus', () => {
  it('should create an instance', () => {
    expect(new CollaborationStatus()).toBeTruthy();
  });
});
