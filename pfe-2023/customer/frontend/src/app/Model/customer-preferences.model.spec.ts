import { CustomerPreferences } from './customer-preferences.model';

describe('CustomerPreferences', () => {
  it('should create an instance', () => {
    expect(new CustomerPreferences()).toBeTruthy();
  });
});
